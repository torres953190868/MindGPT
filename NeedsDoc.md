# BranchMind PRD v0.1

## 1. 产品定位

**BranchMind**：像思维导图一样和 AI 对话。

传统 AI 聊天是线性滚动，适合简单问答；但学习复杂知识时，用户需要不断追问子概念，聊天记录会迅速变长，用户翻阅困难。

本产品将借用mind map的理念，AI 对话拆成可连接、可折叠、可回溯的 **Node**，让用户在画布中浏览知识结构，而不是反复上下翻聊天记录。

---

## 2. 核心痛点

1. 复杂知识是树状结构，但聊天界面是线性结构。
2. 对某个知识点的追问会打断主线内容。
3. 用户难以快速回到某个原始知识点。
4. AI 输出过长后，学习路径不可视化。
5. 用户不知道哪些内容已学、哪些分支还没展开。

---

## 3. 目标用户

- 大学生 / 研究生；
- 自学 AI、编程、金融、法律等复杂领域的用户；
- 用 AI 做论文阅读、研究、写作、知识整理的用户；
- ChatGPT / Claude / Gemini 重度用户；
- 觉得传统 AI 聊天记录太长、太难导航的人。

---

## 4. 核心概念

### Node

Node 是一个独立对话块，包含：

- 标题；
- 用户消息；
- AI 回复；
- 节点摘要；
- 父节点 ID；
- 子节点列表；
- 画布位置；
- 节点内独立对话历史。

### Branch

Branch 是从某个 Node 派生的新节点。

两种主要分支：

1. **Continue Down**：向下继续主线。
2. **Branch Right**：向右深入某个知识点。

### Canvas

Canvas 是节点画布，用户可以在其中查看、点击、拖动、缩放、折叠不同节点。

---

## 5. 核心交互逻辑

BranchMind 不预设固定使用场景，而是围绕每个对话 Node 提供两种基础操作：

### 5.1 向右 New Branch

用户可以从任意 Node 向右创建一个新的平行分支 Node。

适用情况：

- 想深入讨论当前 Node 中的某个概念；
- 想单独展开某句话、某个问题或某个例子；
- 想探索另一个方向，但不想打断当前对话流。

向右分支生成的新 Node 拥有独立对话历史，不会污染原 Node。

### 5.2 向下 Continue

当用户继续和当前 Node 对话时，系统在该 Node 下方创建新的连续 Node。

适用情况：

- 继续当前话题；
- 让 AI 接着讲；
- 对当前回答做进一步追问；
- 沿着当前路径继续推进。

向下生成的新 Node 表示当前对话流的延续。

### 5.3 Node 的通用规则

每个 Node 都可以同时支持：

- 向右创建 New Branch；
- 向下 Continue；
- 保存独立 messages；
- 保留与父节点的连接关系。

整体结构示例：

```
Node A
├─ 右侧：Node B，新分支讨论
└─ 下方：Node C，继续当前对话
```

---

## 6. MVP 必须实现

### 6.1 创建主节点

用户输入主题后，系统创建第一个 Node，并在画布中显示。

验收标准：

- 用户可输入问题；
- AI 可返回回答；
- 回答保存为 Node；
- Node 显示在画布上。

### 6.2 向右创建分支

用户可以在任意 Node 中点击 `New Branch`，系统在当前 Node 右侧创建一个平行探索 Node。

这个新 Node 可用于讨论当前 Node 中的某个子问题、细节、反例、例子或替代方向。

验收标准：

- 新 Node 出现在当前 Node 右侧；
- 新 Node 与原 Node 有连接线；
- 新 Node 继承必要上下文；
- 新 Node 拥有独立对话历史；
- 新 Node 的对话不污染原 Node。

### 6.3 向下继续主线

用户点击 `Continue Down` 后，系统在当前 Node 下方创建新节点。

用途：继续讲下一个知识点、继续当前学习路径。

验收标准：

- 新 Node 出现在当前 Node 下方；
- 表示主线继续；
- 不覆盖旧内容。

### 6.4 节点内独立对话

每个 Node 保存自己的 messages。

验收标准：

- 切换 Node 后能看到该 Node 的历史；
- 不同 Node 的聊天内容互不混合。

### 6.5 画布浏览

用户可以在画布中浏览所有节点。

验收标准：

- 支持拖动、缩放；
- 支持点击节点；
- 当前节点高亮；
- 支持折叠 / 展开子节点。

---

## 7. 暂不做的功能

MVP 暂不做：

- 多人协作；
- 实时同步；
- 插件市场；
- 多模型路由；
- 移动端 App；
- 本地 RAG；
- 自动生成完整课程；
- 复杂权限系统。

---

## 8. 界面设计

### 整体风格：可爱 / 动漫风

- 配色方案：以柔和粉彩色系为主（薰衣草紫、樱花粉、薄荷绿、奶油黄），搭配浅色背景；
- 圆角设计：所有卡片、按钮、面板均使用大圆角（16px+），营造柔软亲切感；
- 字体：使用圆润可爱的字体（如 Nunito、Comfortaa，中文可用圆体）；
- 图标与装饰：使用卡通风格图标（如 Kawaii 表情、小星星、云朵等装饰元素）；
- 动画：节点创建时带有弹跳 / 弹性动画，hover 时轻微浮动，连接线使用虚线 + 渐变色；
- Node 卡片样式：带有轻微阴影和边框渐变，像漂浮的便签纸；
- 吉祥物（可选）：可设计一个小动物助手角色（如猫咪 / 兔子），在画布角落给出提示。

### 推荐布局

```
左侧：节点大纲 / 搜索（带可爱图标装饰）
中间：节点画布（柔和网格背景 + 云朵 / 星星点缀）
右侧：当前节点详情 + 对话输入框（气泡式对话框）
```

### Node 卡片显示

- 标题（圆润字体 + emoji 装饰）；
- 简短摘要；
- 子节点数量（用小徽章气泡显示）；
- 操作按钮（圆形彩色按钮）。

### 核心按钮

- `继续 ↓` 🌱
- `深入 →` 🔍
- `总结` ✨
- `折叠` 🎀
- `删除` 🗑️

---

## 9. AI 行为规则

1. 默认短输出，避免长篇堆叠。
2. 普通解释控制在 300–600 字。
3. 节点摘要控制在 50–100 字。
4. 知识点列表最多 10 个。
5. 创建子节点时，传入：此节点及之前所有对话历史 + 选中文本 + 用户问题。
6. 确保深层分支也能获得完整上下文，后续可根据 token 限制做截断优化。

---

## 10. 数据结构

### Node 示例

```json
{
  "id": "node_001",
  "project_id": "project_001",
  "parent_id": null,
  "title": "强化学习总览",
  "summary": "介绍强化学习核心概念，包括 Agent、Reward、Policy、Q-learning 等。",
  "messages": [
    {
      "role": "user",
      "content": "帮我系统学习强化学习"
    },
    {
      "role": "assistant",
      "content": "强化学习包括以下核心知识点..."
    }
  ],
  "children": ["node_002"],
  "position": {
    "x": 100,
    "y": 200
  },
  "branch_type": "root",
  "created_at": "2026-04-26T12:00:00Z",
  "updated_at": "2026-04-26T12:00:00Z"
}
```

### Branch 示例

```json
{
  "type": "deep_dive",
  "direction": "right",
  "source_text": "Q-learning",
  "instruction": "解释 Q-learning 的核心原理"
}
```

```json
{
  "type": "continue",
  "direction": "down",
  "instruction": "继续讲下一个知识点"
}
```

---

## 11. 技术方案

推荐技术栈：

- Next.js
- React
- TypeScript
- React Flow
- Tailwind CSS
- shadcn/ui
- Zustand
- localStorage / Supabase
- OpenAI API / Anthropic API / Gemini API

MVP 建议：

- 先用 localStorage 保存数据；
- 先用 mock AI response 跑通交互；
- 后续再接真实 AI API 和 Supabase。

---

## 12. 页面结构

### 首页

展示产品定位、Demo 图、创建项目入口。

### 项目列表页

支持新建、查看、删除、搜索项目。

### 工作区页面

核心页面，包含：

- 左侧大纲；
- 中间画布；
- 右侧节点详情；
- 输入框。

---

## 13. MVP 用户流程

### 创建项目

1. 用户点击 `New Project`；
2. 输入主题；
3. 系统创建项目；
4. AI 生成第一个主节点。

### Continue

1. 用户点击当前 Node；
2. 在当前 Node 中继续输入问题或指令；
3. 系统调用 AI 生成回复；
4. 系统在当前 Node 下方创建后续 Node；
5. 新 Node 表示当前对话流的继续。

### New Branch

1. 用户点击当前 Node；
2. 选择当前 Node 中的某个概念、句子、问题或想法；
3. 点击 `New Branch`；
4. 系统调用 AI 生成平行探索内容；
5. 系统在当前 Node 右侧创建新 Node；
6. 新 Node 拥有独立对话历史，不影响原 Node。

### 继续当前对话

1. 用户回到主节点；
2. 点击 `Continue Down`；
3. 输入继续指令；
4. 系统生成下方节点。

---

## 15. 风险与解决方案

### 画布太复杂

解决：节点自动排版，减少手动整理。

### AI 输出仍然太长

解决：默认短输出，细节通过分支展开。

### 节点太多导致混乱

解决：折叠、搜索、大纲、当前路径高亮。

### UI 创新壁垒不足

解决：后续加入复习系统、论文模板、知识掌握度、学习路径等功能。

---

## 17. 开发 AI Prompt

请开发一个名为 BranchMind 的 Web MVP。

它是一个节点式 AI 对话工具，目标是把传统线性 AI 聊天变成可视化知识节点图。

技术栈：Next.js + React + TypeScript + Tailwind CSS + React Flow。数据先存在 localStorage，后续再接 Supabase。

核心功能：

1. 用户可以创建项目；
2. 用户输入问题后，AI 回复并生成 Node；
3. Node 显示在 React Flow 画布中；
4. 用户点击 Node 查看完整对话；
5. 用户点击 `Branch Right`，在右侧生成深入解释节点；
6. 用户点击 `Continue Down`，在下方生成主线继续节点；
7. 每个 Node 保存独立 messages；
8. Node 之间用边连接；
9. 支持拖动、缩放、点击节点；
10. UI 简洁，适合复杂知识学习。

第一版可使用 mock AI response，不必接真实 API。

---

## 18. 结论

BranchMind 的核心价值不是替代 ChatGPT，而是改善 AI 学习交互。

传统聊天是线性的，复杂学习是树状的。本产品要把 AI 对话变成可浏览、可分支、可折叠、可回溯的知识空间。
